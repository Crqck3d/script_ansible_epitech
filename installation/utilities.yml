---

    - name: Installation of user tools
      apt:
        name: "{{ params.tools }}"
        state: present
      when: params.tools is defined and params.tools | length > 0

    - name: Installation of snap packages
      snap:
        name: "{{ params.snap }}"
        classic: yes
      when: params.snap is defined and params.snap | length > 0